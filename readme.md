# Jobly Backend

This is the Express backend for Jobly, version 2.

To run this server:

    npm start

To run server in development mode (requires nodemon):

    npm run dev
    
To run the tests:

    npm test

